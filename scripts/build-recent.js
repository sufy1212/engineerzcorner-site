#!/usr/bin/env node
const fs=require("fs"),path=require("path"),{execSync}=require("child_process"),ROOT=path.join(__dirname,".."),SITE_URL="https://www.engineerzcorner.com";fs.existsSync(path.join(ROOT,".git"))||(console.error(`
\u2717 ABORTING: no .git folder found at the project root.
  This script needs real git history to compute accurate lastmod
  dates. Running it from a zip export or a copy without .git would
  silently stamp today's date on every page \u2014 so it refuses to run.

  Fix: run this from your actual git checkout / Cloudflare Pages
  build environment (which clones the repo with full history),
  not from an extracted zip.
`),process.exit(1));const CONTENT_DIRS=["electrical","mechanical","hvac","civil","automation","solar","general","study-abroad","posts","pm"],SKIP_FILES=new Set(["index.html","notes.html","guides.html","comparisons.html","symbols.html","guides.html","symbol-library.html"]),MAX_ITEMS=10;function listHtmlFiles(dir){const abs=path.join(ROOT,dir);return fs.existsSync(abs)?fs.readdirSync(abs).filter(f=>f.endsWith(".html")&&!SKIP_FILES.has(f)).map(f=>path.join(dir,f)):[]}function gitDate(relPath){try{return execSync(`git log -1 --format=%cI -- "${relPath}"`,{cwd:ROOT,stdio:["pipe","pipe","ignore"]}).toString().trim()||null}catch{return null}}function fileDate(relPath){const g=gitDate(relPath);return g||(console.warn(`  \u26A0 No git history for ${relPath} \u2014 using file mtime instead (commit it to get a real date).`),fs.statSync(path.join(ROOT,relPath)).mtime.toISOString())}function extractTitle(relPath){const html=fs.readFileSync(path.join(ROOT,relPath),"utf8"),h1=html.match(/<h1[^>]*>([\s\S]*?)<\/h1>/i);let title=h1?h1[1]:null;if(!title){const t=html.match(/<title>([\s\S]*?)<\/title>/i);title=t?t[1].split(/—|\|/)[0]:relPath}return title.replace(/<[^>]+>/g,"").trim()}function extractDescription(relPath){const m=fs.readFileSync(path.join(ROOT,relPath),"utf8").match(/<meta\s+name=["']description["']\s+content=["']([\s\S]*?)["']\s*\/?>/i);return m?m[1].trim():""}function escapeXml(str){return String(str).replace(/&(?!amp;|lt;|gt;|quot;|#39;|apos;)/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;")}function cleanPathFor(rel){const base=path.basename(rel);return rel==="index.html"?"":base==="index.html"?rel.slice(0,-10):rel.slice(0,-5)}function listAllHtmlFiles(){const skipDirs=new Set(["assets","scripts",".git"]),results=[];return(function walk(dir){fs.readdirSync(path.join(ROOT,dir),{withFileTypes:!0}).forEach(entry=>{const rel=path.join(dir,entry.name);entry.isDirectory()?skipDirs.has(entry.name)||walk(rel):entry.name.endsWith(".html")&&entry.name!=="404.html"&&results.push(rel)})})("."),results.map(p=>p.split(path.sep).join("/"))}let items=[];CONTENT_DIRS.forEach(dir=>{listHtmlFiles(dir).forEach(relPath=>{const _fd=fileDate(relPath);items.push({t:extractTitle(relPath),u:cleanPathFor(relPath.split(path.sep).join("/")),d:_fd.slice(0,10),_raw:_fd})})}),items.sort((a,b)=>new Date(b._raw)-new Date(a._raw)),items=items.slice(0,MAX_ITEMS).map(({_raw,...rest})=>rest);const outPath=path.join(ROOT,"assets","recent.json");fs.writeFileSync(outPath,JSON.stringify(items,null,2)),console.log(`Wrote ${items.length} item(s) to assets/recent.json:`),items.forEach(i=>console.log(`  ${i.d}  ${i.t}  (${i.u})`));const POSTS_NEW_DAYS=30,postFiles=listHtmlFiles("posts"),postDates=postFiles.map(fileDate),now=new Date,postsTotal=postFiles.length,postsNew=postDates.filter(d=>(now-new Date(d))/864e5<=POSTS_NEW_DAYS).length;let flashcardSymbols=0;const flashcardsPath=path.join(ROOT,"flashcards.html");if(fs.existsSync(flashcardsPath)){const matches=fs.readFileSync(flashcardsPath,"utf8").match(/\{cat\s*:/g);flashcardSymbols=matches?matches.length:0}const stats={postsTotal,postsNew,flashcardSymbols},statsPath=path.join(ROOT,"assets","site-stats.json");fs.writeFileSync(statsPath,JSON.stringify(stats,null,2)),console.log("Wrote assets/site-stats.json:",stats);const allPages=listAllHtmlFiles(),sitemapEntries=allPages.map(rel=>{const base=path.basename(rel),priority=rel==="index.html"?"1.0":base==="index.html"?"0.8":"0.6",cleanPath=cleanPathFor(rel);return`  <url><loc>${SITE_URL}/${cleanPath}</loc><lastmod>${fileDate(rel).slice(0,10)}</lastmod><priority>${priority}</priority></url>`}).sort(),sitemapXml=`<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
${sitemapEntries.join(`
`)}
</urlset>
`;fs.writeFileSync(path.join(ROOT,"sitemap.xml"),sitemapXml),console.log(`Wrote sitemap.xml with ${allPages.length} URLs.`);const feedPosts=postFiles.map(rel=>({title:extractTitle(rel),desc:extractDescription(rel),url:`${SITE_URL}/${rel.split(path.sep).join("/")}`,date:fileDate(rel)})).sort((a,b)=>new Date(b.date)-new Date(a.date)).slice(0,30),feedItems=feedPosts.map(p=>`  <item>
    <title>${escapeXml(p.title)}</title>
    <link>${p.url}</link>
    <guid>${p.url}</guid>
    <description>${escapeXml(p.desc)}</description>
    <pubDate>${new Date(p.date).toUTCString()}</pubDate>
  </item>`).join(`
`),feedXml=`<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0"><channel>
  <title>EngineerzCorneR \u2014 Posts</title>
  <link>${SITE_URL}/posts/index.html</link>
  <description>Deep-dive technical write-ups across Electrical, Mechanical, HVAC, Automation, Civil and Solar.</description>
${feedItems}
</channel></rss>
`;fs.writeFileSync(path.join(ROOT,"feed.xml"),feedXml),console.log(`Wrote feed.xml with ${feedPosts.length} post(s).`);const postsIndexPath=path.join(ROOT,"posts","index.html");if(fs.existsSync(postsIndexPath)){const postsIndexHtml=fs.readFileSync(postsIndexPath,"utf8"),orphans=postFiles.map(rel=>path.basename(rel,".html")).filter(slug=>!postsIndexHtml.includes(`href="${slug}"`));orphans.length&&(console.warn(`
\u26A0 ${orphans.length} post(s) exist but aren't linked from posts/index.html yet:`),orphans.forEach(slug=>console.warn(`  - posts/${slug}.html`)),console.warn(`  (posts/index.html card text is hand-written, so this script won't add
   cards automatically \u2014 just a heads-up so nothing gets missed.)
`))}let healedFiles=0;allPages.forEach(rel=>{const abs=path.join(ROOT,rel),clean=cleanPathFor(rel),correctUrl=`${SITE_URL}/${clean}`;let html=fs.readFileSync(abs,"utf8"),changed=!1;html=html.replace(/(<link rel="canonical" href=")([^"]+)(")/,(m,pre,url,post)=>url!==correctUrl?(changed=!0,pre+correctUrl+post):m),html=html.replace(/(property="og:url" content=")([^"]+)(")/,(m,pre,url,post)=>url!==correctUrl?(changed=!0,pre+correctUrl+post):m);const selfBreadcrumbRe=new RegExp(`("item":\\s*")https://www\\.engineerzcorner\\.com/${rel.replace(/[.*+?^${}()|[\\]\\\\]/g,"\\$&")}(")`);html=html.replace(selfBreadcrumbRe,(m,pre,post)=>(changed=!0,pre+correctUrl+post)),changed&&(fs.writeFileSync(abs,html),healedFiles++,console.warn(`\u26A0 Fixed stale canonical/og:url/breadcrumb URL in ${rel}`))}),healedFiles&&console.warn(`
\u26A0 Self-healed ${healedFiles} page(s) with a stale canonical/og:url/breadcrumb URL.
`);const sitemapHtmlPath=path.join(ROOT,"sitemap.html");if(fs.existsSync(sitemapHtmlPath)){const sitemapHtmlContent=fs.readFileSync(sitemapHtmlPath,"utf8"),missingFromSitemapHtml=postFiles.map(rel=>path.basename(rel,".html")).filter(slug=>!sitemapHtmlContent.includes(`href="posts/${slug}"`));missingFromSitemapHtml.length&&(console.warn(`
\u26A0 ${missingFromSitemapHtml.length} post(s) exist but aren't linked from sitemap.html yet:`),missingFromSitemapHtml.forEach(slug=>console.warn(`  - posts/${slug}.html`)),console.warn(`  (sitemap.html link text is hand-written, so this script won't add
   entries automatically \u2014 just a heads-up so nothing gets missed.)
`))}
